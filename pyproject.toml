[tool.black]
line-length = 88

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F"]

[tool.sqlfluff]
dialect = "postgres"
exclude_rules = "L003"

[project]
name = "osm-caclr-address-dashboard"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary",
    "Jinja2",
    "matplotlib",
]
